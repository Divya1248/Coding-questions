// 18. program to find the ASCII value of a character
const string = prompt("Enter a character: ");
const res = string.charCodeAt(0);
// or result = string.codePointAt(0);

console.log(`The ASCII value is: ${res}`);
